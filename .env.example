# Use a long random value in production. Do not commit the real value.
JWT_SECRET=replace-with-a-long-random-secret
# Set this in Render Environment Variables to the private administrator email.
ADMIN_EMAIL=kinxintruder@gmail.com
# For persistent production storage, replace this with a managed database configuration.
DB_PATH=scorpio.db
